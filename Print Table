## Program takes a list of strings and displays it in a well-organized
## table with each column right-justified.

def printTable():
    for i in range(len(tableData)):
       spam[i] = ' '.join(tableData[i])
    for i in range(len(tableData)):
        colWidths[i] = colWidths[i] + len(spam[i])    
    for i in range(len(spam)):
         print(spam[i].rjust(max(colWidths)))
       


tableData = [['apples', 'oranges', 'cherries', 'banana'],
             ['Alice', 'Bob', 'Carol', 'David'],
             ['dogs', 'cats', 'moose', 'goose']]

colWidths = [0] * len(tableData) # Defines global variable colWidths to be a list
                                 # with as many elements as tableData. Can use this
                                 # to hold the total length of all strings in
                                 # each list inside the list(tableData)
spam = [0] * len(tableData)      # This list will store the new strings, which will be
                                 # the join lists from tableData.
printTable()

